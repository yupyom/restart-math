# 継続TODO

最終更新: 2026-07-15
目的: セッションをまたいでも、教材の意図・未完了の作業・公開条件を取り違えないための作業台帳です。

## 再開時の最初の確認

1. [design/content-architecture.md](design/content-architecture.md) とこのファイルを読む。
2. 編集は `src/` のみで行い、`docs/` を直接編集しない。
3. `npm run check` と `npm run build` を実行してから確認する。
4. 公開前の間は、GitHubへのpushとPages設定の変更を行わない。

## 次回への再開メモ（2026-07-15 追記）

- スマホの表示バグ修正（2026-07-16）：ページ下の「次の単元」ページャーが history.replaceState のみでスクロール処理を持たず、前ページのスクロール位置のまま次の単元が途中から表示されていた。また hashchange 経由の遷移も、html の scroll-behavior: smooth と MathJax 描画の高さ変化が重なってスムーズスクロールが途中で止まっていた。`jumpToTop()`（behavior: "instant" 明示。インラインで scroll-behavior を上書きしても同期的に効かないため）を新設し、単元・問題・読み物・図解への遷移をすべてページ先頭からの即時表示に統一。
- 単元は 57 本に（2026-07-16）：①「関数の記法 f(x) と定義域・値域」（functions の直後。f(2) の読み方、定義域と値域はセット、端点＋単調性で値域を言い切る根拠まで）②「三平方の定理：面積の組み替えで証明する」（geometry と trig-ratios の間。三角形4枚の並べ替えによる答案形式の証明、乗法公式が道具として働く構成、1:2:√3 で三角比へ接続）③「等比数列の和：ずらして引く工夫」（sigma-notation の直後。ガウスの逆順に足すとの対比で「なぜその工夫か」を主題化、複利へ接続）。3本とも context（なぜ？/用語/つながり）つき。glossary の「三平方の定理」を新単元へ付け替え、「斜辺」「定義域」「値域」を追加。
- ブラウザバック対応（2026-07-16）：単元ページャー等の setHashForUnit を pushState 化（同一ハッシュは重複登録しない）。戻る・進むで hashchange → handleRoute が働き、単元をたどった順に再描画＋ページ先頭表示される。他ページは元から location.hash 代入で履歴が残っていた。
- 単元は 60 本に（2026-07-16 第2弾）：④「素因数分解：数の設計図を読む」（identities-equations と powers-roots の間。1を素数に入れない理由＝一意性、360の分解から最大の平方数を読む→ルートの整理へ接続）⑤「相似：形が同じとはどういうことか」（三平方と三角比の間。影で木の高さを測る walkthrough、相似条件①が角2つで済む理由、三角比の「大きさによらない」の土台として明示）⑥「和の式から一般項を取り出す」（等比の和の直後。a_1=S_1・a_n=S_n−S_{n−1}(n≥2) の3手順型、n=1照合の答案作法）。glossary：相似・素因数分解を新単元へ付け替え、相似比・素数を追加。
- 不等号は日本の高校表記（2026-07-16）：MathJax の tex.macros で \le・\leq → \leqq（≦）、\ge・\geq → \geqq（≧）に写像。原稿は \le のままでよく、既存・今後の単元とも自動で2本線表示になる。設定は src/index.html の MathJax config。
- 単元は 62 本に（2026-07-16 第3弾）：⑦「円順列・数珠順列」（combinations-repetition の後、stage 場合の数 5。固定法と割り算法の2ルートで (n−1)! を導き検算、『何を同じとみなすか』を主題化。順列の結び「次の単元では区別をなくすと…」→組合せの導線を壊さない位置に配置）⑧「条件付き確率」（余事象の後、stage 確率 3。P_A(B) 記法、袋の中身の書き換え→定義式で逆算の順、乗法定理と独立の入口。確率の木ラボの戻す/戻さない切替と連携）。**洗い出した単元候補はこれですべて実装済み**。
- 図解候補（未着手）：格子点の最短経路ラボ、Σの階段ラボ、単位半円の三角比ラボ、箱ひげ図ラボ。練習問題候補（未着手）：展開・因数分解・二次方程式・順列組合せ専用・数列（等比・Σ）・箱ひげ図（四分位数）。

- 単元は 45 本に拡充。追加分は、数の分類・絶対値・指数法則・絶対値の方程式不等式（数と式）、ベン図・逆裏対偶・証明とは何か・図形の証明（論理と証明）、順列・組合せ・重複組合せ（counting-principles は樹形図・和積の法則に改訂）、数B新設の数列6本（等差・等比・Σ・階差・漸化式・数学的帰納法）。全単元に定義→理由→心の声つき例の型を踏襲。
- 図解は 19 本。三角形の内角と外角ラボ（頂点を動かして内角の和180°と外角＝残り2内角の和を観察）、ベン図ラボ（重なりの二重カウントと包除原理）を追加済み。
- 範囲に「数B」を新設（scopeカード・単元フィルタ・学習マップ LEVEL 5・カテゴリ「論理・証明」「数列」）。
- 既存単元の点検で、判別式（解の公式のルートの中身として定義）、三角比の記号の読み方、\(\bar{x}\)・\(x_i\)・\(\sum\)、\(P(A)\)、\(\gcd\) 記法、相似の定義を、定義なしで式が出ない形に修正。
- 単元は 52 本に到達。第2弾で、展開と乗法公式・因数分解・二次方程式の解法（解の公式と判別式の出どころ）、平行線と角（錯角による内角の和180°の証明つき）、三角比の拡張（半円の座標での再定義）、余事象と加法定理、四分位数と箱ひげ図を追加。
- 練習問題は 27 種。絶対値・指数法則・等差数列・ベン図の個数の4モードを新設し、それぞれ「はじめの一問」と「少し進んだ問題」の両生成器を実装済み（採点動作もブラウザ確認済み）。順列・組合せは引き続き counting を流用。
- 残る単元候補（洗い出し）：関数の定義域・値域と f(x) 記法 / 相似の単元独立 / 三平方の定理の証明 / 円順列・数珠順列 / 条件付き確率の単元分離 / 素因数分解の単元独立 / 数列の和 \(S_n\) と \(a_n\) の関係 / 等比数列の和の公式。図解候補：格子点の最短経路ラボ、Σの階段ラボ、単位半円の三角比ラボ、箱ひげ図ラボ。
- 残る練習問題候補：展開・因数分解・二次方程式・順列組合せ専用・数列（等比・Σ）・箱ひげ図（四分位数）。
- サイト内検索（#search）を追加。単元・図解・問題・読み物に加えて、単元 context の definitions を「用語」として横断検索できる（JSベース、TeXは平文化して照合）。ナビとホーム索引にも導線を追加。ホーム索引カードの単元数などの固定数値は、増減で古びない表現へ変更。
- 記号の慣習（なぜ大文字のAか・なぜpとqか）を9単元に追記：文字式（大小文字の使い分け総論）、集合、ベン図、逆裏対偶、必要十分、図形の基本、図形の証明（線分2文字・角3文字）、正弦余弦定理（大小のペア対応）、確率（事象は集合の仲間だから大文字）。
- 表示バグ修正：narrative の結論式を = で機械分割して aligned にする処理が、\underbrace のようにかっこ内に = を含む式を壊していた（Misplaced &）。分割片のかっこ対応を確認し、危険なら分割しない fallback を displayEquationTeX に追加。
- 用語の相互リンクを実装：`src/content/glossary.js` の対応表（約80語・手動）＋単元 context の definitions から自動生成した用語索引で、単元本文と読み物の初出用語に点線下線のリンクを張る（`linkifyGlossaryTerms`）。TeX 部分・リンク・ボタン・見出しには触れない。最長一致スパン優先で「二次方程式」の内側の「方程式」だけがリンクされる部分一致を防止。自単元の用語は除外。一般的すぎる語（方程式・計算・整数など）は stopTerms で対象外。用語追加は glossary.js に1行足すだけ（lessonId は npm run check で検証される）。
- 結論式の表示を2モード化（2026-07-16）：displayEquationTeX は、\quad・⟹・\underbrace などの区切りを含む「複数の式・注釈つきの式」は = で揃えず並記モードでそのまま表示し、3a=3×120=360 のような1本の変形チェーンだけを = 縦ぞろえのチェーンモードにする。三角比（sin/cos/tanの並記）・文章から方程式（⟹の後の改行）・データ（平均・中央値・範囲の並記）のずれを解消。チェーンの縦ぞろえは維持を確認。
- 数式崩れの根本修正（2026-07-16）：問題ページなどで数式が生のまま表示され、他ページへ波及する不具合の原因は、typeset対象に親要素（.problem-card）と子要素（learning-actions）を同じバッチで渡していたこと。同じ数式が二重処理され、MathJaxの splitText が IndexSizeError で落ちて描画が止まっていた。flushMathTypeset で「他のターゲットに包含される要素」を除外して解決。あわせて typeset を setTimeout ベース＋直列キュー＋typesetClear に堅牢化し、MathJax は v3 系に固定。連打・ページ往復でも警告0を確認。
- 三角比ラボ：長さを決める辺（斜辺・対辺・隣辺）を選べるようにし、決めた辺は「=」・残りは「≒」表示。30°で斜辺2にすると対辺がちょうど1になり、1:2:√3 が直接見える。有名角の説明文にも「どの辺をいくつにすると比が見えるか」を追記。
- 測量の練習問題（trig-survey「三角比で測る」）を新設：はじめの一問は sin30°・cos60°・tan45° でちょうどの値になる場面（はしご・木の見上げ・ロープ）、少し進んだ問題は60°の見上げで √3 が残る木の高さ（根号のまま→近似値の2段階）。問題は28種に。
- 図解UX改善 第2弾（2026-07-16）：グローバルCSSの `label { display: grid }` が原因で、ラベル内のインライン数式・色タグが縦積みになっていた問題を、ラベル文字列全体を `<span>` で包んで解消（21ラベル）。回答ラベルは display:block 化。三角比ラボは sin/cos/tan で使う2辺だけ強調＋図の下に定義式の大きな表示＋30°/45°/60°プリセット（45°で 1:1:√2 などの有名比を表示）。確率の木の分数を1.4倍に。ベン図問題文の未typeset修正。円周角スライダーは数値表示をやめ「観察点Bを円周にそって動かす」に。互除法SVGに横・たての寸法線と「一辺◯」表記。年率スライダーは18%（上限金利の目安）まで。問題ページは目次を学習順にソート（practice.jsがlessons.jsのorderで並べ替え）、「別の問題を作る」primaryボタン化、ヒント開閉のマージン調整。
- 図解UX改善13件（2026-07-16フィードバック）：ホームに検索窓（送信で#searchへ）／数直線の矢印の頭を拡大／不等号ラボは初期かけ数1・a青とb橙の色タグ／分配法則は図の外側に a・b・c の色付き寸法ラベル＋イコール縦ぞろえの段階式／同類項・方程式・関数のスライダーへ色タグ（方程式は「x袋の数（xの係数）」に改名、関数は y=ax+b 表記へ統一）／ベン図と互除法に問題文を追加、互除法は長方形を正方形で敷きつめるSVG図を新設（手順と色対応）／返済ラボに単利・複利の切替（複利は等比数列、期間を最長60か月に拡張）／会計は「1個の製造費用」表記／速さは車マーカーを「ここまで◯km」・右端を「目盛りの右端600km」に明確化／散らばりラボに外れ値スライダー（平均▲と中央値▼のマーカーが分離して見える）。
- ユーザーフィードバック対応（2026-07-15）：walkthrough に文章の手順（step.text）を追加し、証明3例（偶数の和・図形の合同・連続整数）を答案そのままの文章＋赤の心の声に書き直した。数の分類を序盤（符号ルール直後）へ移し、分数と小数の関係（有限・循環・循環しない）を追記。必要十分条件（necessary-sufficient）と証明の練習（proof-practice、恒等式を道具に使う）を新設し、論理は6単元構成に。等比数列に複利・リボ払いの context カード（教材モデル・判断助言なしの枠内）を追加。単元は 54 本。

## 前回までの再開メモ（2026-07-15）

- 初学者向けに「冗長でていねいな説明」を進める方針を追加。式の解答は、手ごとに式＋赤文字の心の声を並べる `walkthrough` 表示ブロックで書く（例: `{ type: "walkthrough", intro, steps:[{equation, note}], conclusion }`）。`note` は解くときの気持ち（「まず -5 を消す」など）を書き、赤字＋↳で表示される。当たり前に見えることも省かない（例: `y=2x` は `b=0` を省いただけ、を明記）。
- 一次関数のように図が要る単元には `model: { type:"line-graph", a, b, title, description, formula }` を付ける。座標平面に `y=ax+b` を描き、切片 `b` の位置（`b=0` なら原点）と傾きの三角形（右へ1・上へa）を自動表示する。`formula` は等号を一つだけにする（複数の `=` は displayEquationTeX が段組みに崩す）。
- 見本として `functions`（一次関数）と `linear-equations`（一次方程式）を厚く書き直し済み。次は同じ `walkthrough`／`line-graph` を他単元（分配法則、同類項、平方根の整理、不等式、二次関数、連立方程式など）へ横展開する。PC・390px幅で表示確認済み。
- 単元本文は Markdown へ全面移行せず、短い教材を `src/content/` の構造化データとして管理する。今回までに起きた式の崩れは JavaScript の負荷ではなく、段階式・文章題・図の説明を同じ文字列として扱ったことが原因だった。`aligned-steps`、`word-problem`、`narrative`、`walkthrough` の表示ブロックを使い、表示側に意味を推測させない。
- 本文の基本書体は本文 18px、`Noto Sans JP` / `Noto Serif JP`、太さは 700 以下。単元の「おさらいポイント・例・動かして確認・次はここから」は PC でも縦積みで、白と薄いグレーを基本にする。式が特別に長い場合だけ横スクロールを許容する。
- 図解は一度に一つを選んで全幅表示する。すべてのスライダーは現在値を常時表示し、数直線などの図中ラベルも十分な大きさにする。
- 今回までに図解は 17 本。返済（単利の教材モデルであり判断用途ではない）、会計、速さ、平均と散らばりに加えて、集合の数カード、円周角、ユークリッドの互除法を具体物・操作から入る図解として追加済み。対応する単元・問題へ戻るリンクも設定済み。
- 単元は 28 本。学校の章順とは切り離し、`learningPath` で概念の依存順を明示した。文字式→方程式→文章から立式→関数と連立方程式→不等式→恒等式の比較、図形の基本→三角比、平均・散らばり→分散→集合→場合の数→確率の順で進む。原稿配列の置き場所と表示順は分離する。
- 数I / 数A に直接属する 20 単元（新設の「文章から方程式を立てる」を含む）を監査済み。入口ラベルを持つ単元は 2 本で、一部はこの20単元と重なる。全単元に関連図解と基礎問題があり、全 23 問題分野で「はじめの一問」と「少し進んだ問題」を混ぜずに選べる。発展問題の生成コードは `src/assets/js/practice-advanced.js` へ分離した。
- 文字の慣習、三角比、代表値・散らばりの定義を初学者向けに補った。歴史・人物・社会の出典付き読み物は、デカルト、ガウス、ナイチンゲール、パチョーリに、レコード、アル＝フワーリズミ、カントール、アル＝バッターニー、パスカルとフェルマー、ユークリッドを加えた 10 本。全読み物は 17 本で、読み物ページの一覧から横断して選べる。新規歴史読み物の本文は 140〜180 字で 300 字を超えないため、長文用 Markdown 導入の条件にはまだ達していない。
- 数式を含む操作選択は HTML の `option` を使わず、MathJax が組版できるボタン群に置き換えた。数だけの選択肢と通常の文章選択肢は、ネイティブの `select` を維持する。
- トップページはLP風の宣言と「今日の重点」を廃止し、「使い方」と範囲・単元・図解・問題・読み物・学習マップへの全体索引に変更した。単元・図解・問題・読み物は、PCでは共通の左目次＋本文、狭い画面では横スクロール目次になる同じ骨格へ統一した。読み物には本文タイトルとは別の短い `menuTitle` を全17本に設定した。
- 編集対象は `src/` のみ。`docs/` は `npm run build` で更新する。内容・リンク・数式は `npm run check`、構文は `node --check`、差分は `git diff --check` で確認する。
- 次に着手する候補は、高優先度に残る `app.js` の役割分割。発展問題に続いて基礎問題生成、図解、ルーティング、画面描画を挙動を変えずに順次分離する。公開画面はPC幅・390px幅で確認済み。公開後の後始末は完了済み。300字を超える読み物が必要になった時点での Markdown 原稿形式は未完了。

## 現在できていること

- [x] `src/` を編集用、`docs/` をGitHub Pages配信用に分離
- [x] 単元・図解・問題・学習マップを `src/content/` に分離
- [x] 単元から関連図解・関連問題・次の単元へ進む導線
- [x] 図解は一度に一つだけ表示する選択UI
- [x] 本文は明朝系、見出し・目次・操作UIはゴシック系に分離
- [x] 単元本文に現れる孤立した変数と三角比の記号をMathJax表記へ自動整形
- [x] 初期問題は、計算量より規則理解を優先する数値方針をデータ化
- [x] 例を「段階式・文章題・図の説明」の表示ブロックとして区別し、数式の自動整形に意味を推測させない方針を追加
- [x] 図解の数値操作には、スライダーと対応する現在値を常時表示
- [x] 単元ノートの「おさらいポイント・例・動かして確認・次はここから」は、PCでも一列に並べ、白と薄いグレーを基本にした共通カードへ統一
- [x] 学校の章順ではなく、前提概念が先に現れる学習順を `learningPath` として分離
- [x] 方程式の直後に「文章から方程式を立てる」を追加し、単位・同じ総量・検算を明示
- [x] 方程式と恒等式を、用途・成り立つ値の範囲・二つの例で比較
- [x] トップページを「使い方」と全ページへの索引に再構成し、LP風の宣言・「今日の重点」を廃止
- [x] 単元・図解・問題・読み物の目次を、PCでは左メニュー、狭幅では横スクロールになる共通UIへ統一
- [x] 読み物17本に、左メニュー用の短い `menuTitle` を追加

## 優先度: 高

- [x] 単元データに `context`（なぜこの約束？ / 言葉をほどく / どこで役立つ？）を追加し、表示用の開閉カードを作る。
- [x] 計算順序、かっこ、分配法則、等号、負の数を対象に、「困る場面 → 約束 → 確認」の最初の5本の理由コラムを作る。
- [x] 読み物ページ `#stories/<story-id>` と `src/content/stories.js` を追加し、単元から相互リンクする。
- [x] ローン、会計、速さ、データのばらつきを候補に、数値バーで触れる応用図解を1本ずつ設計する。金融・医療の題材は判断の助言にせず、数値関係の説明に限定する。
- [x] 歴史・人物・社会の読み物に出典を持たせる方針と、事実確認のチェックを追加する。
- [x] 図解ページから、対応する単元と問題へ戻るボタンを付ける。
- [x] 問題ページから、対応する単元と図解へ戻るボタンを付ける。
- [x] 範囲ページ（中学 / 数I / 数A）から、該当単元を絞って開く導線を付ける。
- [x] 学習マップのカードを単元データからより直接的に生成し、重複する説明を減らす。
- [x] 単元ごとに「おすすめ図解 1つ」「はじめの一問 1つ」「次の単元 1つ」を教材データ上で明示する。
- [ ] `src/assets/js/app.js` を、画面描画・図解・問題生成・ルーティングの役割ごとに分ける。
  - [x] 「少し進んだ問題」の生成を `src/assets/js/practice-advanced.js` へ分離
  - [ ] 基礎問題生成、図解、ルーティング、画面描画を順次分離
- [ ] 300字を超える読み物・出典付き長文を追加する段階で、見出し・数式・出典の検査を伴う Markdown 原稿形式を導入する。短い単元ノートは構造化データのまま維持する。

## 優先度: 中

- [x] 数I / 数Aの各単元について、図解・初歩問題・復習問題が揃っているかを一覧で監査する。
- [x] 集合と命題、データ分析、図形の性質、数学と人間の活動に、具体物から入る図解を追加する。
- [x] 数学者・科学者・統計や会計の実務に接続する読み物を、単元の理解を補助する長さで追加する。
  - [x] 記号と代数：レコード、アル＝フワーリズミ
  - [x] 集合と確率：カントール、パスカルとフェルマー
  - [x] 図形と三角法：ユークリッド、アル＝バッターニー
  - [ ] 次の候補：負の数・無理数・関数概念・暗号と整数・現代統計の歴史を、各単元の疑問に結び付けて追加
- [x] 問題に「少し進んだ問題」を追加し、最初の問題と混ぜずに選べるようにする。
- [x] 図解・問題の画面で、本文の明朝と数式の見え方をPC・スマホで継続確認する。
  - [x] レスポンシブCSS、数式区切り、重複IDを静的確認
  - [x] 公開画面をPC幅・390px幅で実表示して確認
- [x] 数式を含むタイトル・選択肢の表記方針を整理する。HTMLの`option`内ではMathJaxが使えないため、数式を含む4か所をボタン型UIへ置き換えた。

## 公開直前の確認

- [x] `npm run check` と `npm run build` が通ること
- [x] `docs/` の単元・図解・問題の直接URLを確認すること
- [x] PC・スマホ幅で本文サイズ、数式、ナビゲーションを確認すること
- [x] `docs/` を含めてコミットし、GitHubへpushすること
- [x] GitHub Pagesの配信元を `main` ブランチの `/docs` に切り替えること
- [x] 公開URLで動作を確認すること
- [x] 公開設定に使った一時ファイルの後始末（対応済み）
