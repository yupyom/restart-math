# TODO（未着手・進行中のみ）

完了した変更は [CHANGELOG.md](CHANGELOG.md)、日々の作業手順は [CLAUDE.md](CLAUDE.md)、設計思想は
[design/content-architecture.md](design/content-architecture.md)。このファイルは「これからやること」だけを残す（完了したら消して CHANGELOG へ）。

## 進行中

- （現在なし）

> **単元ブラッシュアップ（依頼11項目）は全項目 完了**（2026-07-23、→ [CHANGELOG.md](CHANGELOG.md)）。開発ドキュメント整備（D1–D6）・完備性の点検と修正（点検 C0–C11／修正 F1–F20）も完了。基盤の分割（単元・図鑑・読み物・図解・出題・CSS）も完了。**進行中の継続作業はなし。**

## 未着手の候補（着手前のバックログ・優先度未定）

### コンテンツ
- （保留）単元「二次関数」の頂点・軸・平方完成を図と対応づけて補強。学びの地図（単元7）とは別に、二次関数単元そのものの図解補強として残す。
- 学びの地図（単元7）から後続単元へ直接リンクしたい（方程式＝linear-equations／不等式＝linear-inequalities／関数＝functions）。今は `context.connections` が labId/storyId/practiceId のみで **lessonId 未対応**なので、地図系の導入単元が「各地点の詳しい単元」へ誘導できない。connections に lessonId 参照を足すか、単元本文に「関連単元」リンク欄を新設するかを検討（気づき：地図単元の実装中に判明）。

### 表示・モバイルUX
- walkthrough の式で、関係記号が1個だけの長い等式（例 \(1000+200u=1600+100u\)）はモバイルで式内横スクロールになる（＝で折り返せないため）。`+`/`-` での改行許容や、モバイルでの微縮小を検討（ページ割れは解消済み・スクロール影のアフォーダンスはあり）。気づき：学びの地図の例題実装中。
- 目次の階層と現在地表示の改善。（現時点は優先度低）
- ダークモードの検討。（現時点は優先度低）

### 検索
- 同義語辞書の拡張、部分一致の重み付け・スコアリング調整。

## 公開直前の確認（リリース時チェックリスト）
- リンク切れ・数式・モバイル導線の最終確認。
- `npm run check` が通る。
- `npm run build` で `docs/` を再生成してからプッシュ。
